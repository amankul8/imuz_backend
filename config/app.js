const mongoose = require("mongoose");

const config = {
    port: 5000,
    url : "mongodb+srv://root:root@cluster0.wky2j.mongodb.net/myFirstDatabase?retryWrites=true&w=majority",
};

module.exports = config;